<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.my.mybatis.mapper.EmployeeMapper">
  <select id="selectEmployee" resultType="com.my.mybatis.bean.Employee">
    select * from t_employee where id = #{id}
  </select>
</mapper>